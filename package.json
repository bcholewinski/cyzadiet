{
  "devDependencies": {
    "cypress": "^8.3.1",
    "cypress-visual-regression": "^1.6.1",
    "dayjs": "^1.10.7"
  },
  "scripts": {
    "cypress-snapshot": "cypress run --env type=base --config screenshotsFolder=cypress/snapshots/base",
    "cypress-open": "cypress open --env type=actual",
    "cypress-tests": "cypress run --env type=actual"
  }
}
